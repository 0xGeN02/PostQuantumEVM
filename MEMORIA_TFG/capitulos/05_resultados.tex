\chapter{RESULTADOS}
\label{cap:resultados}

Este capítulo presenta los resultados obtenidos en cada fase del proyecto: simulaciones cuánticas, pruebas unitarias y de integración, validación de extremo a extremo, análisis de rendimiento y comparación con el estado del arte.


%% ═══════════════════════════════════════════════════════════
\section{Simulaciones cuánticas}
\label{sec:res_qiskit}
%% ═══════════════════════════════════════════════════════════

\subsection{Algoritmo de Shor: recuperación de clave privada}

Se ejecutó la simulación del algoritmo de Shor sobre la curva elíptica reducida $y^2 = x^3 + 7 \pmod{17}$ con generador $G = (12, 1)$ y orden $n = 18$. Para cada ejecución, se genera un par de claves aleatorio $(k, Q = k \cdot G)$ con $k \in \{1, \ldots, 17\}$ y se intenta recuperar $k$ a partir de $Q$.

\textbf{Resultados}:
\begin{itemize}
  \item Con 4.096 \emph{shots}, la clave privada se recupera correctamente en el 100\% de los casos probados (17/17 claves posibles).
  \item El post-procesamiento clásico encuentra al menos un par $(s, t)$ con $\gcd(t, n) = 1$ en cada ejecución, permitiendo el cálculo directo de $k = -s \cdot t^{-1} \bmod n$.
  \item El circuito pedagógico de Qiskit (con QFT de tamaño $2^5 = 32$) tiene una tasa de éxito menor ($\sim$78\%) debido a la fuga espectral causada por $n = 18 \neq 2^k$, confirmando la necesidad de DFT modular exacta para órdenes no potencia de 2.
\end{itemize}

Este resultado valida experimentalmente que el algoritmo de Shor puede recuperar claves privadas ECDSA a partir de claves públicas, demostrando la vulnerabilidad fundamental del esquema sobre una instancia reducida pero algebraicamente equivalente a secp256k1.

\subsection{Algoritmo de Grover: búsqueda de preimágenes}

Se ejecutó la simulación del algoritmo de Grover sobre el Keccak reducido de 4 bits (estado de 8 bits, 2 rondas) con espacio de búsqueda $N = 16$. Para cada hash objetivo $h \in \{0, 1\}^4$ se buscan las preimágenes $x$ tales que $\text{keccak\_toy}(x) = h$.

\textbf{Resultados}:
\begin{itemize}
  \item Para hashes con una única preimagen ($M = 1$): el número óptimo de iteraciones es $\lfloor \frac{\pi}{4}\sqrt{16} \rfloor = 3$, y la preimagen correcta se encuentra con probabilidad $> 95\%$ en 1.024 \emph{shots}.
  \item Para hashes con múltiples preimágenes ($M > 1$): el número de iteraciones se reduce a $\lfloor \frac{\pi}{4}\sqrt{16/M} \rfloor$ y la probabilidad de éxito por medición aumenta.
  \item El speed-up cuadrático se verifica: 3 consultas al oráculo frente a las 8 esperadas de media para búsqueda clásica ($N/2 = 8$).
\end{itemize}

Para Keccak-256 real ($N = 2^{256}$), Grover requeriría $\sim 2^{128}$ consultas frente a $2^{256}$ clásicas. Este nivel de seguridad (128 bits cuánticos) se considera suficiente según las estimaciones actuales.


%% ═══════════════════════════════════════════════════════════
\section{Pruebas unitarias y de integración}
\label{sec:res_unit}
%% ═══════════════════════════════════════════════════════════

La Tabla~\ref{tab:unit_tests} resume las 34 pruebas unitarias implementadas en los \emph{crates} post-cuánticos de pq-reth.

\begin{table}[htbp]
\centering
\caption{Pruebas unitarias por \emph{crate} post-cuántico.}
\label{tab:unit_tests}
\begin{tabular}{@{}lcL{6cm}@{}}
\toprule
\textbf{Crate} & \textbf{Tests} & \textbf{Validaciones principales} \\
\midrule
\texttt{reth-pq-primitives} & 11 & Firma y verificación roundtrip, hash determinista, recuperación de remitente, tipo 0x50, codificación RLP, creación de contrato, formato wallet \\
\texttt{reth-pq-poa} & 9 & Sellado y verificación roundtrip, exclusión de extra\_data del seal hash, detección de cabecera manipulada, clave incorrecta rechazada, rotación round-robin, producción de bloques por turno \\
\texttt{reth-pq-evm} & 6 & ecrecover deshabilitado, ML-DSA presente, precompilados clásicos deshabilitados, precompilados hash activos, cálculo de gas PQHASH, vectores SHAKE-256 conocidos \\
\texttt{reth-pq-precompile} & 5 & Firma válida retorna 0x01, clave incorrecta retorna 0x00, hash manipulado retorna 0x00, longitud incorrecta retorna 0x00, gas insuficiente retorna error \\
\texttt{reth-pq-consensus} & 4 & Transacción válida aceptada, chain\_id incorrecto rechazado, gas\_limit cero rechazado, firma manipulada rechazada \\
\midrule
\textbf{Total} & \textbf{34} & \\
\bottomrule
\end{tabular}
\end{table}

Todas las pruebas unitarias pasan con \texttt{cargo test} en modo \emph{release} (rustc 1.95.0, target x86\_64-unknown-linux-gnu).

Adicionalmente, la biblioteca ml-lattice-rs incluye pruebas de roundtrip para los tres niveles de ML-DSA (44/65/87) y los tres niveles de ML-KEM (512/768/1024), incluyendo detección de mensajes manipulados y rechazo con clave incorrecta.


%% ═══════════════════════════════════════════════════════════
\section{Validación de extremo a extremo}
\label{sec:res_e2e}
%% ═══════════════════════════════════════════════════════════

\subsection{Configuración single-node}

Se ejecutó la suite E2E en modo local con un único nodo \texttt{pq-reth} en modo \emph{dev} (intervalo de bloque: 3 segundos). Resultados: \textbf{12/12 tests pasados} en 26,63 segundos.

\begin{table}[htbp]
\centering
\caption{Resultados E2E en configuración single-node (12/12 tests).}
\label{tab:e2e_single}
\begin{tabular}{@{}llcc@{}}
\toprule
\textbf{Fase} & \textbf{Test} & \textbf{Resultado} & \textbf{Tipo} \\
\midrule
\multirow{3}{*}{Identidad de cadena} & Chain ID = 20561 & PASS & Solo lectura \\
 & Cuentas génesis prefinanciadas & PASS & Solo lectura \\
 & Producción de bloques (height $> 0$) & PASS & Solo lectura \\
\midrule
\multirow{2}{*}{Consenso PoA} & Rotación de validadores & PASS & Solo lectura \\
 & Timestamps monotónicos & PASS & Solo lectura \\
\midrule
\multirow{2}{*}{Comisiones EIP-1559} & Base fee presente & PASS & Solo lectura \\
 & Gas price $> 0$ & PASS & Solo lectura \\
\midrule
\multirow{3}{*}{Transacciones PQ} & Transferencia tipo 0x50 (sig=3.309 B) & PASS & TX \\
 & Recibo válido (status=0x1) & PASS & TX \\
 & Incremento de nonce & PASS & TX \\
\midrule
\multirow{2}{*}{Contratos inteligentes} & Despliegue exitoso (contract\_address) & PASS & TX \\
 & Llamada correcta (retorno 0x42) & PASS & TX \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Configuración multi-nodo}

Se ejecutó la suite E2E con un clúster de 3 nodos validadores en modo PoA (slot time: 3 segundos), conectados mediante pares confiables. Resultados: \textbf{15/15 tests pasados} en 29,98 segundos. El clúster alcanzó el bloque 6 en 22 segundos.

\begin{table}[htbp]
\centering
\caption{Resultados E2E adicionales en configuración multi-nodo (3/3 tests).}
\label{tab:e2e_multi}
\begin{tabular}{@{}lcc@{}}
\toprule
\textbf{Test} & \textbf{Resultado} & \textbf{Observación} \\
\midrule
Chain ID consistente & PASS & 20561 en los 3 nodos \\
Altura de bloque consistente & PASS & Drift $\leq$ 2 bloques \\
Estado consistente (saldos) & PASS & Saldos idénticos en los 3 nodos \\
\bottomrule
\end{tabular}
\end{table}


%% ═══════════════════════════════════════════════════════════
\section{Análisis de rendimiento}
\label{sec:res_performance}
%% ═══════════════════════════════════════════════════════════

Los benchmarks se ejecutaron con Criterion.rs en modo \emph{release} con soporte AVX2, sobre 100 muestras por operación.

\subsection{Operaciones criptográficas}

La Tabla~\ref{tab:crypto_bench} compara la latencia de las operaciones criptográficas de ECDSA/secp256k1 con ML-DSA-65.

\begin{table}[htbp]
\centering
\caption{Comparativa de latencia: ECDSA/secp256k1 vs. ML-DSA-65.}
\label{tab:crypto_bench}
\begin{tabular}{@{}lccc@{}}
\toprule
\textbf{Operación} & \textbf{ECDSA ($\mu$s)} & \textbf{ML-DSA-65 ($\mu$s)} & \textbf{Ratio} \\
\midrule
Generación de claves & 35,1 & 208,5 & 5,94$\times$ más lento \\
Firma & 41,4 & 342,8 & 8,28$\times$ más lento \\
\textbf{Verificación} & \textbf{49,2} & \textbf{42,0} & \textbf{0,85$\times$ (14\% más rápido)} \\
\bottomrule
\end{tabular}
\end{table}

El resultado más significativo es que la \textbf{verificación ML-DSA-65 es un 14\% más rápida que la verificación ECDSA}. Esto es crítico para un cliente blockchain, donde cada nodo debe verificar todas las transacciones de cada bloque. La mayor latencia de firma (8,28$\times$) afecta al usuario del wallet pero no al rendimiento de la red.

\subsection{Funciones hash}

La Tabla~\ref{tab:hash_bench} compara SHAKE-256 con Keccak-256 para diferentes tamaños de entrada.

\begin{table}[htbp]
\centering
\caption{Comparativa de latencia: Keccak-256 vs. SHAKE-256.}
\label{tab:hash_bench}
\begin{tabular}{@{}rccc@{}}
\toprule
\textbf{Entrada} & \textbf{Keccak-256 (ns)} & \textbf{SHAKE-256 (ns)} & \textbf{Ratio} \\
\midrule
32 B & 300 & 545 & 1,82$\times$ \\
64 B & 304 & 560 & 1,84$\times$ \\
128 B & 301 & 573 & 1,90$\times$ \\
256 B & 555 & 802 & 1,45$\times$ \\
512 B & 1.089 & 1.322 & 1,21$\times$ \\
1 KB & 2.054 & 2.329 & 1,13$\times$ \\
4 KB & 8.130 & 8.100 & 1,00$\times$ \\
8 KB & 16.229 & 15.608 & 0,96$\times$ \\
\bottomrule
\end{tabular}
\end{table}

SHAKE-256 es $\sim$1,8$\times$ más lento para entradas pequeñas (32--128 bytes) debido a su menor tasa ($r = 1.088$ bits vs. $r = 1.088$ bits para Keccak-256, pero con diferente padding). Sin embargo, las latencias convergen para entradas mayores de 4 KB y SHAKE-256 es marginalmente más rápido a 8 KB. Para el caso de uso principal ---derivación de direcciones (entrada de 1.952 bytes)---, el overhead es de $\sim$1,1$\times$, prácticamente negligible.

\subsection{Tamaño de transacciones}

\begin{table}[htbp]
\centering
\caption{Comparativa de tamaño de transacción: clásica vs. post-cuántica.}
\label{tab:tx_size}
\begin{tabular}{@{}lcc@{}}
\toprule
\textbf{Componente} & \textbf{Clásica (bytes)} & \textbf{PQ (bytes)} \\
\midrule
Nonce + gas + value & 30 & 53 \\
Dirección destino & 20 & 20 \\
Firma & 65 $(v, r, s)$ & 3.309 \\
Clave pública & (implícita en firma) & 1.952 \\
\midrule
\textbf{Total mínimo} & \textbf{$\sim$61} & \textbf{$\sim$5.314} \\
\textbf{Ratio} & \multicolumn{2}{c}{$\sim$87$\times$} \\
\bottomrule
\end{tabular}
\end{table}

El incremento de 87$\times$ en tamaño de transacción es el coste principal de la migración post-cuántica. De los 5.253 bytes adicionales, el 63\% corresponde a la firma (3.309 B) y el 37\% a la clave pública (1.952 B).

La Figura~\ref{fig:tx_size_comparison} muestra visualmente la diferencia de tamaño entre ambos tipos de transacción.

\begin{figure}[htbp]
\centering
\begin{tikzpicture}
\begin{axis}[
  ybar stacked,
  bar width=1.8cm,
  width=10cm,
  height=7cm,
  ylabel={Tamaño (bytes)},
  symbolic x coords={Clásica, Post-Cuántica},
  xtick=data,
  ymin=0,
  ymax=6000,
  legend style={at={(0.98,0.98)}, anchor=north east, font=\small},
  nodes near coords style={font=\tiny},
  every node near coord/.append style={anchor=south},
]

% Classical
\addplot+[fill=blue!30, draw=blue!50] coordinates {(Clásica, 30) (Post-Cuántica, 53)};
\addplot+[fill=green!30, draw=green!50] coordinates {(Clásica, 20) (Post-Cuántica, 20)};
\addplot+[fill=red!30, draw=red!50] coordinates {(Clásica, 65) (Post-Cuántica, 3309)};
\addplot+[fill=orange!30, draw=orange!50] coordinates {(Clásica, 0) (Post-Cuántica, 1952)};

\legend{Campos (nonce/gas/value), Dirección, Firma, Clave pública}
\end{axis}
\end{tikzpicture}
\caption{Comparativa de tamaño de transacción por componente. La firma ML-DSA-65 (3.309 B) y la clave pública (1.952 B) constituyen el 99\% del incremento.}
\label{fig:tx_size_comparison}
\end{figure}

\subsection{Impacto en gas y throughput}

\begin{table}[htbp]
\centering
\caption{Impacto en gas y capacidad de la red.}
\label{tab:gas_impact}
\begin{tabular}{@{}lcc@{}}
\toprule
\textbf{Métrica} & \textbf{Clásica} & \textbf{PQ} \\
\midrule
Gas por transferencia & 21.976 & 42.892 \\
Gas de verificación criptográfica & 3.000 (ecrecover) & 3.450 (ML-DSA) \\
Gas de calldata (firma + pk) & 65 $\times$ 16 = 1.040 & 5.261 $\times$ 16 = 84.176 \\
Gas total mínimo & 21.976 & 105.176 \\
TPS máximo (30M gas, 12 s/bloque) & $\sim$119 & $\sim$58 \\
Pérdida de throughput & --- & $\sim$51\% \\
Crecimiento anual de cadena & $\sim$27 GB & $\sim$2,3 TB \\
\bottomrule
\end{tabular}
\end{table}

La pérdida de throughput del 51\% se debe principalmente al coste de \emph{calldata} de la firma y la clave pública (84.176 gas adicionales). Estrategias de mitigación para producción incluyen: registro de claves públicas on-chain (elimina 1.952 B tras la primera transacción), compresión de claves ML-DSA, y un gas limit mayor para cadenas PQ-nativas.

\subsection{Calibración de gas del precompilado}

El coste de gas del precompilado ML-DSA-65 (\texttt{0x0100}) se calibra como:
\begin{equation}
  \text{gas} = \lceil \text{latencia}_{\mu\text{s}} \times \text{ratio gas/}\mu\text{s} \times \text{factor seguridad} \rceil = \lceil 42 \times 61 \times 1,35 \rceil = 3.450.
\end{equation}

Este valor es competitivo con \texttt{ecrecover} (3.000 gas) pese a que ML-DSA-65 opera sobre claves y firmas significativamente mayores, gracias a la eficiencia de la aritmética de retículos en hardware moderno con AVX2.


%% ═══════════════════════════════════════════════════════════
\section{Comparación con el estado del arte}
\label{sec:res_comparison}
%% ═══════════════════════════════════════════════════════════

La Tabla~\ref{tab:comparison} sitúa este trabajo en el contexto de las propuestas existentes de blockchain post-cuántica.

\begin{table}[htbp]
\centering
\caption{Comparación con trabajos relacionados.}
\label{tab:comparison}
\begin{tabular}{@{}L{2.5cm}L{2cm}L{2cm}cccc@{}}
\toprule
\textbf{Trabajo} & \textbf{Plataforma} & \textbf{Algoritmo} & \textbf{Nativo} & \textbf{Consenso PQ} & \textbf{Multi-nodo} & \textbf{EIP} \\
\midrule
Kiktenko et al. \parencite{quantumsafeblockchain2018} & Custom & QKD + hash & Sí & No & No & No \\
Fernández-Caramés \parencite{quantumresistance2023nature} & Análisis & --- & --- & --- & --- & No \\
Berdik et al. \parencite{surveycomparison2023} & Survey & --- & --- & --- & --- & No \\
Lee et al. \parencite{quantumsafefabric2024} & Fabric & ML-DSA & Sí & No & Sí & No \\
EIP-7932 \parencite{eip7932} & Ethereum & Genérico & No* & No & --- & Sí \\
EIP-8051 \parencite{eip8051} & Ethereum & ML-DSA & No* & No & --- & Sí \\
\textbf{Este trabajo} & \textbf{Ethereum (reth)} & \textbf{ML-DSA-65} & \textbf{Sí} & \textbf{Sí (PoA)} & \textbf{Sí (3 nodos)} & \textbf{Sí} \\
\bottomrule
\end{tabular}
\end{table}

\noindent *EIP-7932 y EIP-8051 operan a nivel de precompilado/contrato, no como tipo de transacción nativo.

Las diferencias clave de este trabajo respecto al estado del arte son:

\begin{enumerate}
  \item \textbf{Tipo de transacción nativo}: Es la primera implementación que define un tipo EIP-2718 (\texttt{0x50}) para transacciones post-cuánticas en Ethereum, frente a las propuestas de verificación a nivel de contrato inteligente.
  
  \item \textbf{Consenso post-cuántico}: Implementa un mecanismo de consenso (PoA) con sellado ML-DSA-65, eliminando la dependencia de BLS12-381 en la capa de bloque. Los trabajos previos en Ethereum no abordan el consenso.
  
  \item \textbf{Implementación completa}: Incluye cliente de ejecución modificado, wallet, EVM extendida (opcode + precompilado), infraestructura de despliegue y suite E2E. Las propuestas existentes (EIP-7932, EIP-8051) son especificaciones sin implementación funcional.
  
  \item \textbf{Validación multi-nodo}: Demuestra la viabilidad en un clúster de 3 nodos con propagación de bloques, rotación de validadores y consistencia de estado.
  
  \item \textbf{Validación cuántica}: Incluye simulaciones de Shor y Grover sobre modelos reducidos, demostrando experimentalmente las amenazas teóricas.
\end{enumerate}


%% ═══════════════════════════════════════════════════════════
\section{Análisis de resultados}
\label{sec:res_analysis}
%% ═══════════════════════════════════════════════════════════

Los resultados permiten extraer las siguientes conclusiones parciales:

\textbf{Viabilidad criptográfica}: La verificación ML-DSA-65 (42 $\mu$s) es más rápida que la verificación ECDSA (49,2 $\mu$s), lo que demuestra que la migración post-cuántica no implica una degradación del rendimiento de verificación ---la operación más crítica para un nodo blockchain. La firma es 8,3$\times$ más lenta, pero solo afecta al usuario final, no a la red.

\textbf{Impacto en tamaño}: El incremento de 87$\times$ en tamaño de transacción es significativo pero manejable. Las firmas ML-DSA-65 (3.309 B) son más compactas que las de SLH-DSA (7.856 B) y Falcon-512 (666 B) presenta problemas de implementación segura. ML-DSA-65 ofrece el mejor equilibrio tamaño-seguridad-implementabilidad.

\textbf{Impacto en throughput}: La pérdida del 51\% de TPS se debe principalmente al coste de calldata, no a la verificación criptográfica. Un registro de claves públicas (eliminando 1.952 B por transacción recurrente) reduciría esta pérdida a $\sim$35\%.

\textbf{Validación E2E}: El 100\% de las pruebas (27/27 entre single-node y multi-node) pasan satisfactoriamente, demostrando que la integración de ML-DSA-65 en un cliente de ejecución Ethereum es viable sin romper las propiedades fundamentales del protocolo.

\textbf{Consenso PoA}: La rotación round-robin con sellado ML-DSA-65 funciona correctamente en un clúster de 3 nodos, con un drift máximo de 2 bloques y convergencia de estado. Este resultado valida el consenso post-cuántico, aunque la escalabilidad a centenares de validadores (necesaria para PoS) requiere esquemas de agregación que no existen aún.

La Figura~\ref{fig:quantum_timeline} sitúa temporalmente la urgencia de la migración.

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[
  >={Stealth},
]

% Timeline arrow
\draw[->, very thick, gray!60] (0, 0) -- (14, 0) node[right, font=\small] {Años};

% Year marks
\foreach \y/\lbl in {0/2026, 2/2028, 4/2030, 6/2032, 8/2034, 10/2036, 12/2038} {
  \draw[thick] (\y, -0.15) -- (\y, 0.15);
  \node[below, font=\tiny] at (\y, -0.2) {\lbl};
}

% Migration bar (6-13 years, optimistic to pessimistic)
\draw[fill=blue!20, draw=blue!50, thick] (0, 0.6) rectangle (6, 1.2);
\node[font=\footnotesize, align=center] at (3, 0.9) {Migración (optimista): 6 años};

\draw[fill=blue!10, draw=blue!30, thick] (6, 0.6) rectangle (13, 1.2);
\node[font=\footnotesize, align=center] at (9.5, 0.9) {Migración (pesimista): +7 años};

% CRQC bar
\draw[fill=red!20, draw=red!50, thick] (10, -1.2) rectangle (13, -0.6);
\node[font=\footnotesize, align=center] at (11.5, -0.9) {CRQC disponible};

% CRQC range
\draw[{Stealth[length=3pt]}-{Stealth[length=3pt]}, red!60, thick] (10, -0.4) -- (13, -0.4);
\node[font=\tiny, above, red!70] at (11.5, -0.35) {10--15 años (estimación NIST)};

% Harvest now
\draw[fill=yellow!20, draw=yellow!60, thick] (0, -1.2) rectangle (10, -0.6);
\node[font=\footnotesize, align=center] at (5, -0.9) {<<Harvest now, decrypt later>>};

% This work marker
\draw[thick, green!60!black] (0, -0.05) -- (0, 1.5);
\node[font=\footnotesize\bfseries, green!60!black, above] at (0, 1.3) {Este trabajo};

% Danger zone
\draw[fill=red!5, draw=none] (10, 0.5) rectangle (13, 1.3);
\node[font=\tiny, red!70, rotate=90] at (13.3, 0.9) {riesgo};

\end{tikzpicture}
\caption{Timeline cuántico: la ventana de migración (6--13 años) se solapa con la estimación de disponibilidad de un CRQC (10--15 años). La amenaza <<harvest now, decrypt later>> comienza hoy.}
\label{fig:quantum_timeline}
\end{figure}
